<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-feature android:name="android.hardware.type.watch" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Rest timer must fire on time with the screen off; see plan Phase 5. -->
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />

    <application
        android:name=".LiftWearApplication"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.DeviceDefault">

        <uses-library
            android:name="com.google.android.wearable"
            android:required="false" />

        <!-- Standalone: works without a paired phone for everything except key delivery. -->
        <meta-data
            android:name="com.google.android.wearable.standalone"
            android:value="true" />

        <!--
          singleTask: the Tile, the Ongoing Activity chip and the complication all open this
          from the system UI, and without it each tap stacked another full copy of the app
          onto the task - three were found on a Galaxy Watch 4, each with its own nav host,
          ViewModels and network refresh, and swiping one away revealed the next.
        -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:taskAffinity=""
            android:theme="@android:style/Theme.DeviceDefault">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
          Receives the API key from the phone companion. The path filter is literal, not a
          prefix: /liftwear/credentials/ack is a message meant for the phone, and this
          service has no business waking for it.
        -->
        <!--
          Runs for the life of a workout so the rest timer exists outside the app.
          specialUse, not health: health requires a sensor permission, and LiftWear reads
          no sensors. Android 14+ throws on a type mismatch, so this is enforced.
        -->
        <service
            android:name=".rest.WorkoutSessionService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="@string/fgs_special_use_justification" />
        </service>

        <!--
          Reads the Room cache only - a Tile request is time-boxed and routinely arrives
          with the app dead, so there is nothing to await.
        -->
        <service
            android:name=".tile.LiftWearTileService"
            android:exported="true"
            android:label="@string/tile_label"
            android:description="@string/tile_description"
            android:permission="com.google.android.wearable.permission.BIND_TILE_PROVIDER">
            <intent-filter>
                <action android:name="androidx.wear.tiles.action.BIND_TILE_PROVIDER" />
            </intent-filter>
            <meta-data
                android:name="androidx.wear.tiles.PREVIEW"
                android:resource="@drawable/tile_preview" />
        </service>

        <!--
          Reads Room and the rest controller only. UPDATE_PERIOD_SECONDS is 0 because
          nothing here changes on a timer: the rest countdown is time-dependent text the
          system animates, and everything else is pushed with an update request.
        -->
        <service
            android:name=".complication.LiftWearComplicationService"
            android:exported="true"
            android:label="@string/complication_label"
            android:icon="@drawable/ic_notification"
            android:permission="com.google.android.wearable.permission.BIND_COMPLICATION_PROVIDER">
            <intent-filter>
                <action android:name="android.support.wearable.complications.ACTION_COMPLICATION_UPDATE_REQUEST" />
            </intent-filter>
            <meta-data
                android:name="android.support.wearable.complications.SUPPORTED_TYPES"
                android:value="SHORT_TEXT,RANGED_VALUE" />
            <meta-data
                android:name="android.support.wearable.complications.UPDATE_PERIOD_SECONDS"
                android:value="0" />
        </service>

        <!-- Fires the rest buzz whether or not the app is open. -->
        <receiver
            android:name=".rest.RestAlarmReceiver"
            android:exported="false" />

        <!--
            No android:permission here, deliberately. Declaring
            com.google.android.gms.permission.BIND_LISTENER_SERVICE gates who may *bind* to
            this service, and Google Play services on a Galaxy Watch 4 does not hold it - so
            every delivery was refused with "Permission Denial: ... requires
            com.google.android.gms.permission.BIND_LISTENER_SERVICE" and a SecurityException
            inside WearableService, none of it visible to this app. The key then only ever
            arrived via SetupViewModel's sweep, i.e. never while the app was closed, which is
            the case this service exists for.
        -->
        <service
            android:name=".sync.CredentialListenerService"
            android:exported="true">
            <intent-filter>
                <action android:name="com.google.android.gms.wearable.DATA_CHANGED" />
                <data
                    android:scheme="wear"
                    android:host="*"
                    android:path="/liftwear/credentials" />
            </intent-filter>
        </service>
    </application>
</manifest>
